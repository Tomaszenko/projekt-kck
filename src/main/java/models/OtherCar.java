package models;

public class OtherCar extends Car {
    public OtherCar(int meters, int maxSpeed, int speed, CarColor carColor, CarLane carLane, CarDirection carDirection) {
        super(meters, maxSpeed, speed, carColor, carLane, carDirection);
    }
}
